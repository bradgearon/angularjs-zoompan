define('controllers/creeper', [
    'config'
],
    function (app) {
        app.controller('CreeperCtrl', ['$scope',
            function (scope) {
                
            }
        ]);
    }
);